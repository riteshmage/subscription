<?php 
if($this->subscrptionStatus($this->getProductid())){ ?>
<div id="div_subscription">
	<?php 
		$discountDetails = $this->discountDetail();
		// echo "<pre>";print_r($discountDetails);die;
		if(!empty($discountDetails)){
	?>
	Subscrption:-
	<select>
		<option value="">Select for subscription</option>
		<?php foreach($discountDetails as $key => $value) { ?>
		<option value="<?php $key ?>"><?php echo $value['subscription_unit'].'-'.$value['discount_type'].'--'.$value['discount_value']; ?> </option>
		<?php } ?>
	</select>
</div>
<?php } } ?>